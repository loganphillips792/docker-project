apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: host-default
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/host-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/host-default: "true"
